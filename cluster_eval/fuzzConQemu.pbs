#!/bin/sh
#PBS -N FuzzingQemu
#PBS -o fsf.stdout.$PBS_JOBID
#PBS -e fsf.stderr.$PBS_JOBID

. ./youEdit.sh

../testtrace -c $FSF_NUM_INPUTS -i $FSF_DUMP -s $FSF_SIZES -f /tmp/res_qemu_$PBS_JOBID.txt -o /tmp/outDIR_$PBS_JOBID -Q -- ./djpeg @@

. ./exitCleanup.sh
