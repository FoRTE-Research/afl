#!/bin/sh
#PBS -N FuzzingDyninst
#PBS -o fsf.stdout.$PBS_JOBID
#PBS -e fsf.stderr.$PBS_JOBID

. ./youEdit.sh

../testtrace -i $FSF_DUMP -s $FSF_SIZES -f ./res_${FSF_BENCH}_dyninst_$PBS_JOBID.txt -o ./outDIR_$PBS_JOBID -c $FSF_NUM_INPUTS -- ./${FSF_BENCH}OriginalDyninst $FSF_BENCH_ARGS

. ./exitCleanup.sh
